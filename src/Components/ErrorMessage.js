import React from 'react';

const ErrorMessage = () => {
    return (
        <div>
            <p>Error: Something went wrong. Please try again!</p>
        </div>
    );
};

export default ErrorMessage;